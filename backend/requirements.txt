# Vectyra AI Backend — dependencies
# Install: pip install -r requirements.txt

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP (replaces blocking `requests`)
httpx>=0.27.0

# PDF parsing
PyMuPDF>=1.24.0

# Database (optional)
supabase>=2.4.0

# AWS Lambda adapter (optional — only needed for Lambda deployments)
# mangum>=0.17.0

# Dev / testing
python-multipart>=0.0.9
